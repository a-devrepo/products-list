import FilterCard from '../FilterCard';
import './styles.css';

export default function ListingBody() {
    return (
        <main>
            <section className='container'>
                <FilterCard />
            </section>
        </main>
    )
}
